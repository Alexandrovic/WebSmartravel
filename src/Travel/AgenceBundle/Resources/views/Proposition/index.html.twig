{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Voyage list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Moyentransport</th>
                <th>Destination</th>
                <th>Budget</th>
                <th>Nbplace</th>
                <th>Datedepart</th>
                <th>Dateretour</th>
                <th>Typesortie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('travel_proposition_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.moyenTransport }}</td>
                <td>{{ entity.destination }}</td>
                <td>{{ entity.budget }}</td>
                <td>{{ entity.nbPlace }}</td>
              
                <td>{% if entity.dateDepart %}{{ entity.dateDepart|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.dateRetour %}{{ entity.dateRetour|date('Y-m-d H:i:s') }}{% endif %}</td>
              
                <td>{{ entity.typesortie }}</td>
               
                <td>
                <ul>
                    <li>
                        <a href="{{ path('travel_proposition_show', { 'id': entity.id }) }}">show</a>
                    </li>
               
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('travel_proposition_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
