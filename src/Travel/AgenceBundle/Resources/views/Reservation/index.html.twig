{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reservation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datereservation</th>
                <th>Payement</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('travel_reservation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateReservation %}{{ entity.dateReservation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.payement }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('travel_reservation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('travel_reservation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('travel_reservation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
