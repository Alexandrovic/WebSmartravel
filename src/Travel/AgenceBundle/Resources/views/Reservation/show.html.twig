{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reservation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Datereservation</th>
                <td>{{ entity.dateReservation|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Payement</th>
                <td>{{ entity.payement }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('travel_reservation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('travel_reservation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
