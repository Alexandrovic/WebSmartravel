{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Facture</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Sommefacture</th>
                <td>{{ entity.sommeFacture }}</td>
            </tr>
            <tr>
                <th>Datepayementfacture</th>
                <td>{{ entity.datePayementFacture|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('travel_facture') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('travel_facture_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
